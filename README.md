Installation & Setup

Follow the steps below to set up and run the project locally:

1. Install Dependencies

Run the following command to install all required packages: npm install

2. Start the Development Server

After the installation is complete, start the development server using: npm run dev

Once started, the application will be available at: http://localhost:5173/
